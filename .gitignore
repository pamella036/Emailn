# Binaries for programs and plugins
*.exe
*.exe~
*.dill
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when with liteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/
/temp
.env